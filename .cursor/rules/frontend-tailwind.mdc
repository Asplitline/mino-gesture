---
alwaysApply: true
---
你正在修改前端和 Tailwind 样式。

前端要求：
- 优先复用现有组件
- 状态设计完整：loading / empty / error / disabled
- 视图状态留在前端，本地临时状态不要无意义上提
- 组件职责清晰，避免巨型组件

Tailwind 要求：
- 优先 utility class
- class 顺序按 layout / spacing / typography / color / state 组织
- 相同视觉模式重复 3 次以上再抽组件或样式
- 避免过长 class 串，必要时抽辅助函数或组件
- 响应式和暗色模式跟随现有项目约定

交互要求：
- 桌面应用优先考虑键盘操作、滚动体验、窗口尺寸变化
- 动画和过渡保持克制
- UI 改动说明交互前后差异


其他要求：
1. 白色主视觉并支持暗黑模式

- 样式保持简洁、留白充足，默认浅色主题以白色为主（背景、卡片、输入框优先浅色系）。
- 必须同时支持暗黑模式；新增组件时同时提供 light/dark 配色，不允许只做单主题。
- 颜色数量控制在必要范围，优先中性色和低饱和强调色，避免花哨渐变和重装饰。
- 组件样式应可复用（通过 Tailwind 组合类或抽取 class 片段），避免重复长类串。
- 交互态完整：`hover`、`focus`、`disabled` 在 light/dark 下都要可读且对比度达标。
- 页面视觉层级简洁：标题、正文、辅助信息三级即可，避免“仪表盘化”堆信息。
